import 'package:flutter/material.dart';

class ColorManager{
  static const primaryColor = Color(0xFF835DF1);
  static const primaryColorDark = Color(0x00141414);
  static const white = Colors.white;
}